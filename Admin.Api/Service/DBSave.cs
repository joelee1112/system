using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace Admin.Dist.Service
{
    public class DBSave<T>
    {

        //public T getClass(T myclass)
        //{
        //    if (myclass != null)
        //    {
        //        if (Guid.Empty == myclass)
        //        {
        //            db.OffStreetImageUrl.Add(_OffStreetImageUrl);
        //        }
        //        else
        //        {
        //            db.Entry(_OffStreetImageUrl).State = EntityState.Modified;
        //        }
        //        //int? v = db.Version.Find(c => c.Version1 == 1);
        //        //_offStreetCarPark.Version = v.HasValue ? v.Value + 1 : 0;

        //        //AddNewVersion(_offStreetCarPark, v);
        //        db.SaveChanges();
        //    }
        //    return array[index];
        //}

    }
}
