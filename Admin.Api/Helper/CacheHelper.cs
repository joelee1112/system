
using Microsoft.AspNetCore.Http;
using Microsoft.Extensions.DependencyInjection;

namespace Admin.Dist.Helper
{
    public class CacheHelper
    {
        //private IHttpContextAccessor contextAccessor;

        //private RoleCacheHelper _Role;
        //private PermissionCacheHelper _Permission;

        //public CacheHelper(IHttpContextAccessor contextAccessor)
        //{
        //    this.contextAccessor = contextAccessor;
        //}

        //public RoleCacheHelper Role => _Role = _Role ?? new RoleCacheHelper(contextAccessor, this);
        //public PermissionCacheHelper Permission => _Permission = _Permission ?? new PermissionCacheHelper(contextAccessor, this);
    }
}
